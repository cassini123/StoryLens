# Volcengine IAM credentials for Jimeng (visual.volcengineapi.com).
# Access Key ID usually starts with AKLT (IAM console → API access keys).
# Paste Secret Access Key exactly as shown (including any = padding). Do not base64-decode it.
JIMENG_ACCESS_KEY=
JIMENG_SECRET_KEY=

# CHItest participant JSON uploads (Vercel Production).
# Prefer forwarding to your Lighthouse so you can SSH in and ls the files:
CHITEST_FORWARD_URL=http://101.34.248.192:8787
CHITEST_VIEW_TOKEN=
# Optional fallback instead of Lighthouse:
CHITEST_GITHUB_TOKEN=
