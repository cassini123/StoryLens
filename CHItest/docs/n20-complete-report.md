# CHItest N=20 完整报告

日期：2026-09-10。包来自 `CHItest/data/participants/`。  
分析脚本：`CHItest/scripts/analyze_packets.py`。人表：`CHItest/docs/n20-person-table.csv`。  
论文对照短版：`CHItest/docs/n20-paper-analysis.md`。

不把原文 prompt、生成图或可识别昵称写进本文件。实验室对包请用 zip 标签。

---

## 1. 到包情况

| zip | 可解析 | 填写 ID | 组 | 7 题结束 | export_ready | 正式效力 |
| --- | --- | --- | --- | --- | --- | --- |
| P001–P011 | 是 | 见人表 | 见人表 | 是 | P003 否 | 见下 |
| P012 | 是 | P001 | 控制 | 是 | 是 | **是**（从 P021 还原） |
| P013–P016 | 是 | 多为 P001 / P088 | 1 控 3 架 | 是 | 是 | 是 |
| P017–P018 | 是 | 见人表 | 脚手架 | 是 | 否（timing） | 否 |
| P019 | 是 | 见人表 | 脚手架 | **6/7** | 否 | 否 |
| P020 | 是 | 见人表 | 脚手架 | **5/7** | 否 | 否 |

20 场 `started_at` 全不同，不是复制粘贴的同一 json。  
11 包填写 ID 都是 `P001`：按 zip 标签当独立被试。

收集到的组别（按 zip）：脚手架 13、控制 7。正式样本已平衡。

---

## 2. 效力分层

和协议一致，分三层，不要混用。

1. **机器戳 `export_ready`**：complete + 全部 study flags + 无 issues。
2. **正式效力（本文主样本）**：7 题结束 + `formal-between-v2` + 有 validation 且 `export_ready` + Sketch 未进 API。
3. **敏感度**：只要 7 题结束且 Sketch 未进 API（含 timing 未闭合、v1）。

| 层 | n | scaffold | control | 名单 |
| --- | --- | --- | --- | --- |
| 可解析会话 | 20 | 13 | 7 | P001–P020 |
| 正式效力 | **14** | **7** | **7** | P001, P002, P005–P016 |
| 敏感度 complete7 | 18 | 11 | 7 | 正式 14 + P003, P004, P017, P018 |
| 不完整 | 2 | 2 | 0 | P019, P020 |

相对 N=11 正式 n=9（S4/C5）：新进正式的是 P012–P016。

排除不进正式效力：

- P003、P017、P018：`timing_complete` 失败。P003 / P017 各有 1 次 `generation_recovery`（生成挂住后刷新才离开）。
- P004：v1，无 `validation.json` / `event_log.csv`，T0 仍带 target spec。
- P019：第 2 道 T2 未 `ended_at`，但两道 T3 已做完（中途换题）。
- P020：两道 T2 都未结束，T0–T1 和两道 T3 已做完。T2 上各有 6 次和 3 次成功生成，更像卡住后跳题，而不是没生成。

---

## 3. 过程检查（20 场可解析）

| 检查 | 结果 |
| --- | --- |
| `sketch_sent` | 全部 false |
| `api_input` | 全部 `image+text` |
| `input_sketch_snapshot_id` | 全部空 |
| 任务顺序 | v2 包与组别匹配（架：T0 T1 T1 T2 T2 T3 T3；控：T0 T1×4 T3 T3） |
| 已结束 T2 的 loop | Sketch 操作 + Auto Prompt + 成功生成，均有 |
| 专家评分 / P_norm | 无 |

并发失败仍集中在 P001、P002、P009、P011（以及 P007/P008/P016 各 1 次）。这是生成服务限流，不是效力排除标准。

---

## 4. 论文对照数字（次级指标）

主 DV `P_norm` 不存在，下面 **不能写成功效结论**。

公式：

```text
Primary  = (S_middle − S_early) − (C_middle − C_early)
Transfer = S_T3 − C_T3
```

### 正式 n=14

| 指标 | Primary | S Δ | C Δ | Transfer | gΔ | bootstrap Primary |
| --- | --- | --- | --- | --- | --- | --- |
| Self-alignment 1–7 | **−0.64** | −0.14 | +0.50 | −0.07 | −0.47 | −1.86 … 0.64 |
| Result-alignment 1–7 | −0.21 | +0.36 | +0.57 | +0.43 | −0.15 | −1.43 … 1.21 |
| 墙钟秒（去 ≥30 min） | **+395** | +259 | −136 | +193 | 1.38 | 160 … 680 |
| 终稿字数 | +19 | +6 | −13 | +1 | 0.95 | 0.3 … 37 |

自评：控制组 middle 涨、脚手架组持平或略降。时间：脚手架 middle 更贵（T2 含草图），区间不含 0。字数：脚手架 middle 略长。

### 敏感度 n=18

Self Primary ≈ −0.59，Transfer ≈ 0。去异常墙钟 Primary ≈ +252 s。结论方向与正式样本一致。

块均值（正式；self / result / 去异常墙钟秒）：

| | S early | S middle | S T3 | C early | C middle | C T3 |
| --- | --- | --- | --- | --- | --- | --- |
| Self mean | 5.07 | 4.93 | 4.79 | 5.14 | 5.64 | 4.86 |
| Result mean | 4.93 | 5.29 | 5.36 | 5.00 | 5.57 | 4.93 |
| 墙钟 median (s) | 212 | 451 | 220 | 258 | 85 | 186 |

---

## 5. 新 9 包（P012–P020）单独看

| 包 | 怎么用 |
| --- | --- |
| P012 | 正式效力。控制组，约 25 min，11/11 生成成功。GitHub 把 P021 改名为 P012 时曾写成空文件，已从 `P021-packet.zip` 还原。 |
| P013–P016 | 正式效力。四场都较短（约 16–41 min），生成失败很少。 |
| P017–P018 | 7 题写完但 timing flag 不过。可进敏感度，不进正式。 |
| P019–P020 | 不完整。P020 的 T2 有多次成功生成却没有结束题，和「生成卡住只能换题」的现场报告相符。线上已修等待超时/取消等待；这 2 人若同一浏览器还有进度，可做完 T2 再导一次，**不要重新开始**。 |

---

## 6. 建议

1. **先打专家 P_norm**（正式 14，有余力再打敏感度 18）。没有它就没有主分析。
2. P019/P020 不完整：能补则补（同一浏览器做完 T2，不要重新开始），不能补就保持排除。
3. 收数据时看填写 ID，不要让连续多人都停在 P001。
4. 正式样本已经 7 vs 7。不要用 T2>T1 或自评上涨声称脚手架有效。
