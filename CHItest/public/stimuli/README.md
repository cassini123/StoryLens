Replace these SVG stills with the 20 photographs from `画面与描述.docx` using the same filenames:

IMG01.svg → IMG01.jpg (or keep .svg and update `CHItest/data/images/images.json` `file` fields)

The Word document path provided to the cloud agent was a local WeChat cache file and was not available in this environment.
