Downloaded expert ratings belong here after export. Keep one record per expert; do not store means only.

Anonymous expert-agent scores (480 rows, Expert 01–04, no name slugs) are in
`anonymous/`. Rebuild the standalone ZIP with:

```bash
python3 CHItest/scripts/export_expert_agent_ratings.py
```

Do not copy researcher keyfiles or expert name-slug folders into that export.
