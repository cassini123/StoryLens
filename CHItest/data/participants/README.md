Downloaded participant records belong here. The running app stores sessions in the browser until export.

Do not commit raw participant zips (prompts, images, typed IDs). Keep them local.

Paper-protocol analysis of the first 11 packets: `CHItest/docs/n11-paper-analysis.md`. Re-run:

```text
CHITEST_PACKET_DIR=/path/to/unpacked python3 CHItest/scripts/analyze_packets.py
```

