# Stimulus images

Group folders match `data/tasks/stimuli.json`:

- `environment/` E01–E05
- `character_space/` C01–C04
- `camera/` A01–A05
- `composition/` D01–D06

These copies are SVG placeholders. Replace with the original photographs using the same filenames when the Word-doc stills are available. Vite serves the public copies from `CHItest/public/data/tasks/images/`.
