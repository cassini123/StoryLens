# Stimulus files

See `stimuli.json`, `target_modifications.json`, and `images/README.md`. Task rows for assignment are derived from `stimuli.json`; do not keep a separate 8-task Direct/Sketch list. `target_modifications.json` is researcher-only (current visual state + intended modification) and is never shown on the participant UI.
