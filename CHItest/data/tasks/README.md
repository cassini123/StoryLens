# Stimulus files

See `stimuli.json`, `target_modifications.json`, and `images/README.md`. Assignment is stratified (7 unique images). Scaffold and control groups use the same pool; only stage labels differ. `target_modifications.json` is researcher-only and is never shown on the participant UI.
